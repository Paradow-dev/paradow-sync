# Paradow Sync

Paradow Sync is a lightweight Windows desktop tool for launching and organizing multiple Dofus Unity clients per team profile, with class-icon overlays and focus management across monitors.

**Build requires Windows + .NET 8** (WinUI 3 app and Win32 interop targets are not supported on Linux).
